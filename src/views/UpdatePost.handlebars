<h1>Postagem</h1>
<form action="/update/{{id}}" method="post">
    <p>Título: </p>
    <input type="text" name="titulo" placeholder="{{titulo}}">
    <p>Conteúdo: </p>
    <textarea name="conteudo" placeholder="{{conteudo}}"></textarea>
    <button type="submit">Postar</button>
</form>