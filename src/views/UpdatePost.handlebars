<h1>Postagem</h1>

<form action="/update/{{id}}" method="post">
    <p>Título: </p>
    <input type="text" name="titulo" value="{{titulo}}">
    <p>Conteúdo: </p>
    <textarea name="conteudo" rows="5">{{conteudo}}
    </textarea>
    <button type="submit">Postar</button>
</form>